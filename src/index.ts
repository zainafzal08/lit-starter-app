/**
 * index.ts
 *
 * This is the entry point of the application, i.e the first bit of ts
 * that gets run. We use it to bootstrap our app by loading the
 * app-view element. This import statemebt ensures that the custom element is
 */
import './app-view.ts';
